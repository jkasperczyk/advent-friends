<!DOCTYPE html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Zmiana hasła</title>
    <link rel="stylesheet" href="/css/tailwind.css" />
  </head>
  <body class="min-h-screen bg-slate-900 text-white p-4">
    <header class="flex items-center justify-between mb-4 gap-2">
      <h1 class="text-xl font-bold">🔐 Zmiana hasła</h1>
      <div class="flex gap-2">
        <a href="/calendar" class="bg-slate-700 px-3 py-2 rounded text-sm">Kalendarz</a>
        <a href="/logout" class="bg-slate-700 px-3 py-2 rounded text-sm">Wyloguj</a>
      </div>
    </header>
    <div class="max-w-md mx-auto bg-slate-800/60 rounded-xl p-4 space-y-3 border border-slate-700">
      <% if (error) { %>
        <p class="bg-red-500/80 px-3 py-2 rounded text-sm"><%= error %></p>
      <% } %>
      <% if (success) { %>
        <p class="bg-emerald-500/80 px-3 py-2 rounded text-sm"><%= success %></p>
      <% } %>
      <form method="POST" action="/change-password" class="space-y-3">
        <div>
          <label class="block mb-1 text-sm">Obecne hasło</label>
          <input type="password" name="currentPassword" class="w-full bg-slate-900 border border-slate-700 rounded px-3 py-2 text-sm" required />
        </div>
        <div>
          <label class="block mb-1 text-sm">Nowe hasło</label>
          <input type="password" name="newPassword" class="w-full bg-slate-900 border border-slate-700 rounded px-3 py-2 text-sm" required minlength="6" />
        </div>
        <div>
          <label class="block mb-1 text-sm">Powtórz nowe hasło</label>
          <input type="password" name="confirmPassword" class="w-full bg-slate-900 border border-slate-700 rounded px-3 py-2 text-sm" required minlength="6" />
        </div>
        <button class="w-full bg-xmas-green py-2 rounded text-sm font-semibold">Zapisz nowe hasło</button>
      </form>
      <p class="text-xs text-slate-400">Hasło musi mieć min. 6 znaków. Konto: <strong><%= user.username %></strong></p>
    </div>
  </body>
</html>
