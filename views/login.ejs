<!DOCTYPE html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <title>Logowanie – Wyzwanie Świąteczne 2025</title>
    <link rel="stylesheet" href="/css/tailwind.css" />
  </head>
  <body class="min-h-screen flex items-center justify-center bg-gradient-to-b from-slate-900 via-slate-950 to-slate-900 text-white px-4 py-8">
    <div class="w-full max-w-sm flex flex-col items-center gap-4">
      <div class="text-center">
        <h1 class="text-3xl font-bold tracking-tight drop-shadow-lg leading-tight">
          <span class="text-3xl md:text-4xl bg-gradient-to-r from-emerald-300 via-red-200 to-amber-200 bg-clip-text text-transparent drop-shadow">
            Wyzwanie Świąteczne 2025
          </span>
        </h1>
      </div>
      <div class="bg-slate-900/40 backdrop-blur-sm p-6 rounded-2xl w-full border border-white/5 shadow-lg">
        <% if (error) { %>
          <p class="bg-red-500/80 p-2 rounded mb-3 text-center text-sm"><%= error %></p>
        <% } %>
        <form method="POST" action="/login" class="space-y-3">
          <div>
            <label class="block mb-1 text-sm">Użytkownik</label>
            <input name="username" class="w-full bg-slate-950/70 border border-slate-700 rounded px-3 py-2 text-sm" required autofocus />
          </div>
          <div>
            <label class="block mb-1 text-sm">Hasło</label>
            <input type="password" name="password" class="w-full bg-slate-950/70 border border-slate-700 rounded px-3 py-2 text-sm" required />
          </div>
          <button class="w-full bg-xmas-green py-2.5 rounded mt-2 text-sm font-semibold shadow">
            Zaloguj się
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
